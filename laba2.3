#include <iostream>
#include <windows.h>
#include <math.h>
using namespace std;

int main () 
{
	double x,y;
	cout << "enter x " << endl;
	cin >> x;
	
	if (x <= -6 && x >= -9 )
	{
		y = -sqrt(9-pow(x+6,2) );
		cout << "y=" << y << endl;
	}
	else if (x >= -6 && x <= -3 )
	{
		y = x + 3;
		cout << "y=" << y << endl;
	}
	else if (x >= -3 && x <= 0 ) {
		y = sqrt(9 - pow(x, 2));
		cout << "y= " << y << endl;
	}
	else if (x >= 0 && x <= 3 ) {
		y = -x+3;
		cout << "y=" << y << endl;
	}
	else if (x >= 3 && x <= 9 )
	{
		y =(x-3)/2;
		cout << "y=" << y << endl;
	}
	else cout << "net otveta" << endl;
	
	return 0;
	
	
}
