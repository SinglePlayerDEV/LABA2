#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	int a;
	cout << "etner number a  " << endl;
	cin >> a;

	if (a >= 0) {
		a = a + 1;
	}
	else a = a;
	
	cout << "a = " << a << "\n" <<endl;
	
	return 0;
	
}
